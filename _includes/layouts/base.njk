<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or metadata.title }}</title>
    <link rel="stylesheet" href="{{ '/dist/index.css' | url }}">
</head>
<body>
    {{ content | safe }}
</body>
</html>