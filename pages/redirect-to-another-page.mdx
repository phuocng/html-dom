import { PostLayout } from '../layouts/PostLayout';

```js
location.href = '/the/new/url';
```

### See also

-   [Reload the current page](/reload-the-current-page)

export default ({ children }) => (
    <PostLayout keywords="" title="Redirect to another page">
        {children}
    </PostLayout>
);